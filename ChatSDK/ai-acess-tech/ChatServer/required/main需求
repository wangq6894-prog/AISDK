main方法需求:
1.gflags库定义ServerConfigs所需要的参数，参数可以通过命令行进行提供
    - 服务器绑定地址，默认0.0.0.0，端口8080
    - 日志级别，默认INFO
    - deepseek,chatgpt等云端的模型的api_key在环境变量中配置，不需要通过命令行参数提供，变量名分别为DEEPSEEK_API_KEY,CHATGPT_API_KEY,GEMINI_API_KEY
    - 温度值，默认0.7，范围0.0-1.0，最大token数2048
    - 参数可以通过命令行提供，也可以通过配置文件ChatServer.conf加载，配置文件位置在ChatServer的目录下
    - 
2.生成main方法
    - 通过gflags库解析命令行参数，参数可能来自命令行或配置文件ChatServer.conf
    - 对配置文件进行安全检查
3.给用户提供ChatServer的帮助说明
    -当用户输入 ./AIChatServer -h 或 ./AIChatServer --help 时，打印帮助说明
    - 当用户输入 ./AIChatServer -v 或 ./AIChatServer --version 时，打印版本信息，默认版本为1.0.0
    - 使用说明中显示AIChatServer的参数选项的说明，以及使用案例，以及ChatServer提供的接口的说明

要求：将生成的main.cpp文件放置在ChatServer目录下
